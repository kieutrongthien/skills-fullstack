# Migration checklist (NestJS)

- Are new migrations added and named clearly?
- Does config include necessary env vars and validation?
- Have you run migrations locally and in lower envs?
- Do tests/e2e cover the new schema paths?
- Is rollback safe and tested where possible?
